// Write a function `wordReverse` that accepts a single argument, 
// a string. The function should return a string with the order of the words reversed. 
// Don't worry about punctuation.

// ```
// console.log(wordReverse("Ishmael me Call"));

// => "Call me Ishmael"

function wordReverse(str) {
    console.log(revArray.reverse());
  }
  revArray = ["Ishmael", "Call me"];
  wordReverse(revArray);