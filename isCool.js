// Write a function `isCool` that accepts one parameter, `name` as an argument. 
// The function should return a string that outputs the name and a message
// saying that the person is super cool.

// console.log(isCool("Thom"));

// => "Thom is super cool";

function isCool(name) {
    let text = "is super cool"
    console.log(name + " " + text)
}
isCool("Sarah")