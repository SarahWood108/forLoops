// Write a function `twoLengths` that accepts two parameters (strings). 
// The function should return an _array_ of numbers where each number 
// is the length of the corresponding string.
// // example usage
// twoLengths("Hank", "Hippopopalous");
// => [4, 13]

function twoLengths(name1,name2) {
    console.log([name1.length, name2.length]);
  }
  twoLengths("Kali","Saraswati")

